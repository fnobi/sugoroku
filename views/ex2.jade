extends layout

block content
  section#sugoroku-ex
    form(action="/ex/" + exid + '/next', method="POST")
      h1 問2
      p
        最初に表示される画面を、「home」ではなく「profile」にしてみて下さい。

      p
        ul
          li
            エディター:
            a(href=editorURL, target="_blank")= editorURL
          li
            実行:
            a(href=demoURL, target="_blank")= demoURL

      input(type="hidden", name="exnum", value=exnum)
      input(type="submit", value="できた! (またはパス)")